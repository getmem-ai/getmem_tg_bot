# Runtime dependencies for `pip install -r requirements.txt`.
# (pyproject.toml is the source of truth; this mirrors it for plain-pip/Docker.)
aiogram>=3.13,<4
httpx>=0.27
aiosqlite>=0.20
python-dotenv>=1.0

# Long-term memory SDK — vendored locally until published to PyPI.
# Installs the package under third_party/getmem-ai.
./third_party/getmem-ai
