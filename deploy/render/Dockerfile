# Public web entry for the Render Blueprint: Caddy reverse-proxying the private
# api + miniapp services. Build context is this directory (deploy/render).
FROM caddy:2-alpine
COPY Caddyfile /etc/caddy/Caddyfile
